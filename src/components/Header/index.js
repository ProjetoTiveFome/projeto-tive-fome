import React from 'react'

import Menu from '../Menu'

import './header.css'

const Header = () => (
  <header className='header'>
    <nav className='menu'>
      <Menu />
    </nav>
  </header>
)

export default Header;
